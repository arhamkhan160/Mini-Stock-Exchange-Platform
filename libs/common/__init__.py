"""Shared kernel for all Mini Stock Exchange microservices.

Import as `common.<module>` (PYTHONPATH includes the `libs/` directory).
This package is the CONTRACT between services. Do not change it without
telling every team member — a silent change here breaks the whole system.
"""

__version__ = "1.0.0"
