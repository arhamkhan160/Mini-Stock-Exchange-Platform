# Extras for the Matching Engine only.
# The shared, pinned dependency set lives in requirements-base.txt at the repo
# root and is installed alongside this file:
#
#   docker : RUN pip install -r requirements-base.txt -r service-requirements.txt
#   local  : pip install -r requirements-base.txt -r services/matching-engine/requirements.txt
#
# (No extra dependencies needed — and no database driver is used at runtime.)
